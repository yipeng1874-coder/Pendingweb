# 主播转岗数据归属改造方案

> 文档版本：v2.1  
> 创建时间：2026-06-17  
> 更新时间：2026-06-17  
> 背景：解决主播跨厅转岗后，历史任务数据"随人走"导致的组织归属错乱问题，并完善审计追踪、账号安全隔离及主播身份生命周期彻底终结机制

---

## 一、问题背景

### 1.1 核心问题描述

当主播小明从 **A 厅** 转入 **B 厅** 后，系统现有逻辑会：

1. 将 `AnchorProfile.hallOrgId` 直接更新为 B 厅 ID
2. 将小明名下所有 `UserIdentity`（`roleCode=ANCHOR`）的 `orgId` 批量更新为 B 厅
3. 将小明历史所有 `TaskRecord` 的 `subjectOrgId` 批量更新为 B 厅

**后果：**
- A 厅管理员永久失去了小明在 A 厅时产生的历史任务数据
- B 厅凭空"继承"了小明在 A 厅的所有历史数据，违背真实业务归属

### 1.2 问题代码定位

**文件：** `backend/src/modules/anchor/service.ts` → `updateProfile()` 方法

```typescript
// 第 508 - 521 行（问题根源）
if (nextHallOrgId !== current.hallOrgId) {
  // 问题1：直接修改身份的 orgId，破坏了历史身份的时间切片
  await tx.userIdentity.updateMany({
    where: { anchorProfileId: updated.id, roleCode: "ANCHOR" },
    data: { orgId: nextHallOrgId, scopePath: nextHallPath },
  });
  
  // 问题2：强制将所有历史任务的 subjectOrgId 改为新厅，历史数据被"洗白"
  await tx.taskRecord.updateMany({
    where: {
      OR: [
        ...(updated.boundUserId ? [{ subjectUserId: updated.boundUserId }, { userId: updated.boundUserId }] : []),
        ...(current.douyinUid ? [{ subjectKey: current.douyinUid }] : []),
      ],
    },
    data: { subjectOrgId: nextHallOrgId },
  });
}
```

**文件：** `backend/src/modules/task/record/record.service.ts` → `ensureDailyRecordForDate()` 方法

```typescript
// 第 198 - 214 行（生成记录时未写入组织快照）
record = await prisma.taskRecord.create({
  data: {
    assignmentId: assignment.id,
    userId: user.id,
    identityId: identity.id,
    subjectType: "USER",
    subjectKey,
    subjectUserId: user.id,
    subjectName: user.nickname,
    templateVersion: assignment.templateVersion ?? assignment.template.version,
    recordDate,
    deadlineAt: getDailyTaskSupplementDeadline(recordDate),
    status: "pending",
    totalItems: assignment.template.items.length,
    doneItems: 0,
    // ❌ 此处缺少 subjectOrgId 的写入
  },
});
```

---

## 二、设计原则

### 2.1 核心原则：业务事件不可篡改

> **"已经发生过的业务事件，其所属组织归属必须在发生时永久固化，不得因主播的身份变动而事后修改。"**

### 2.2 数据模型层次关系

```
User（账号，全局唯一）
  └── UserIdentity（身份，一个账号可有多个身份）
        ├── roleCode: ANCHOR | ADMIN | ...
        ├── orgId: 所属组织（身份生效时绑定）
        └── status: active | disabled（时效状态）

AnchorProfile（主播档案，抖音 UID 全局唯一）
  └── hallOrgId: 当前所属厅（档案层面的归属）

TaskRecord（任务记录）
  ├── identityId → UserIdentity（产生记录时的身份）
  ├── subjectOrgId → OrgUnit（产生记录时的组织 ID，快照）
  └── subjectKey: 记录的业务主键
```

---

## 三、改造方案：身份版本化（Identity Versioning）

### 3.1 方案核心思路

**废弃"修改"操作，改为"失效旧身份 + 新建新身份"。**

| 操作 | 原逻辑 | 新逻辑 |
| :--- | :--- | :--- |
| 主播转岗 | `UPDATE UserIdentity SET orgId = B厅` | `UPDATE UserIdentity SET status='disabled'` + `INSERT UserIdentity (orgId=B厅)` |
| 历史任务 | `UPDATE TaskRecord SET subjectOrgId = B厅` | **不操作**（历史数据保持原状） |
| 任务生成 | 不写入 `subjectOrgId` | 写入 `subjectOrgId = identity.orgId` |

### 3.2 转岗后数据状态示意

```
小明（User）
  ├── 身份-1（已失效）roleCode=ANCHOR | orgId=A厅 | status=disabled | expiredAt=转岗时间
  │     └── TaskRecord-1, TaskRecord-2, ...（所有在A厅时产生的历史记录）
  │
  ├── 身份-2（活跃） roleCode=ANCHOR | orgId=B厅 | status=active
  │     └── TaskRecord-3, TaskRecord-4, ...（转岗后在B厅产生的记录）
  │
  └── 身份-3（活跃） roleCode=ADMIN | orgId=某团队 | status=active（管理身份不受影响）
```

### 3.3 关于数据唯一性的说明

`UserIdentity` 表的唯一索引为：

```prisma
@@unique([userId, roleCode, orgId, anchorProfileId])
```

由于新旧身份的 `orgId` 不同（A厅 vs B厅），两条记录**可以在数据库中合法共存**，不会触发唯一性冲突。

---

## 四、具体改造步骤

### 步骤一：修复任务记录生成逻辑（低风险，立即可做）

**文件：** `backend/src/modules/task/record/record.service.ts`

**改动位置：** `ensureDailyRecordForDate()` 函数中的 `prisma.taskRecord.create()` 调用

**改动内容：** 新增 `subjectOrgId` 字段的写入

```typescript
// 改动前
record = await prisma.taskRecord.create({
  data: {
    assignmentId: assignment.id,
    userId: user.id,
    identityId: identity.id,
    subjectType: "USER",
    subjectKey,
    subjectUserId: user.id,
    subjectName: user.nickname,
    templateVersion: assignment.templateVersion ?? assignment.template.version,
    recordDate,
    deadlineAt: getDailyTaskSupplementDeadline(recordDate),
    status: "pending",
    totalItems: assignment.template.items.length,
    doneItems: 0,
  },
});

// 改动后
record = await prisma.taskRecord.create({
  data: {
    assignmentId: assignment.id,
    userId: user.id,
    identityId: identity.id,
    subjectType: "USER",
    subjectKey,
    subjectUserId: user.id,
    subjectName: user.nickname,
    templateVersion: assignment.templateVersion ?? assignment.template.version,
    recordDate,
    deadlineAt: getDailyTaskSupplementDeadline(recordDate),
    status: "pending",
    totalItems: assignment.template.items.length,
    doneItems: 0,
    // ✅ 新增：锁定任务产生时的组织归属快照
    subjectOrgId: identity.orgId ?? undefined,
    subjectOrgType: identity.orgId ? "HALL" : undefined,
  },
});
```

**影响评估：** 零风险。字段本就存在于数据库中，只是补充写入，不影响任何现有查询逻辑。

---

### 步骤二：改造转岗逻辑（核心改动）

**文件：** `backend/src/modules/anchor/service.ts`

**改动位置：** `updateProfile()` 方法，第 508~522 行

**改动内容：**

```typescript
// ============ 改动前（有问题的代码）============
if (nextHallOrgId !== current.hallOrgId) {
  // 直接修改身份 orgId（会导致历史数据漂移）
  await tx.userIdentity.updateMany({
    where: { anchorProfileId: updated.id, roleCode: "ANCHOR" },
    data: { orgId: nextHallOrgId, scopePath: nextHallPath },
  });
  // 暴力改写历史任务归属（根本问题所在）
  await tx.taskRecord.updateMany({
    where: {
      OR: [
        ...(updated.boundUserId ? [{ subjectUserId: updated.boundUserId }, { userId: updated.boundUserId }] : []),
        ...(current.douyinUid ? [{ subjectKey: current.douyinUid }] : []),
      ],
    },
    data: { subjectOrgId: nextHallOrgId },
  });
}

// ============ 改动后（身份版本化方案）============
if (nextHallOrgId !== current.hallOrgId) {
  const transferTimestamp = new Date();
  const bkSuffix = `_bk_${transferTimestamp.toISOString().slice(0, 10).replace(/-/g, "")}`;

  // 1. 将旧主播档案的关键唯一字段加 bk 后缀，释放唯一键占位，并物理锁死该档案
  await tx.anchorProfile.update({
    where: { id: current.id },
    data: {
      nickname:   `${current.nickname}(bk)`,
      douyinNo:   current.douyinNo  ? `${current.douyinNo}${bkSuffix}`  : undefined,
      douyinUid:  current.douyinUid ? `${current.douyinUid}${bkSuffix}` : undefined,
      status:     "transferred",  // 专属终态，不可逆转
    },
  });

  // 2. 同步更新旧 TaskRecord 关联键（确保历史任务依然能链接到旧档案）
  if (current.douyinUid) {
    await tx.taskRecord.updateMany({
      where: { subjectKey: current.douyinUid },
      data:  { subjectKey: `${current.douyinUid}${bkSuffix}` },
    });
  }

  // 3. 失效当前主播身份
  await tx.userIdentity.updateMany({
    where: {
      anchorProfileId: updated.id,
      roleCode: "ANCHOR",
      status: "active",
    },
    data: {
      status: "disabled",
      expiredAt: transferTimestamp,
    },
  });

  // 4. 在新厅创建全新的主播身份
  if (updated.boundUserId) {
    await tx.userIdentity.create({
      data: {
        userId: updated.boundUserId,
        roleCode: "ANCHOR",
        orgId: nextHallOrgId,
        anchorProfileId: updated.id,
        scopePath: nextHallPath,
        status: "active",
      },
    });
  }

  // 5. 写入转岗审计日志
  await tx.anchorTransferLog.create({
    data: {
      anchorId:       anchor.id,
      fromOrgId:      current.hallOrgId,
      toOrgId:        nextHallOrgId,
      fromProfileId:  current.id,
      toProfileId:    updated.id,
      transferTime:   transferTimestamp,
      transferReason: transferReason ?? null,   // 由接口入参传入
      operatorId:     currentAdminId,
    },
  });

  // 6. ✅ 不再执行 TaskRecord.subjectOrgId 的批量更新
  //    历史数据永远归属于原组织，这是正确的行为
}
```

---

### 步骤三：存量历史数据修复脚本

对于在改造上线前已经产生的、`subjectOrgId` 为空的 `TaskRecord` 记录，需要执行一次性数据修复。

**修复逻辑：** 根据 `TaskRecord.identityId` 关联到 `UserIdentity`，取 `UserIdentity.orgId` 回填到 `TaskRecord.subjectOrgId`。

**脚本思路（Prisma 版本）：**

```typescript
// scripts/backfill-subject-org-id.ts
// 用途：对存量 subjectOrgId 为空的任务记录，根据身份关联补填组织ID
async function backfillSubjectOrgId() {
  const records = await prisma.taskRecord.findMany({
    where: {
      subjectOrgId: null,
      identityId: { not: null },
    },
    include: {
      // 注意：identity 关联可能需要根据实际 schema 关系调整
    },
    take: 500, // 分批处理，避免锁表
  });

  for (const record of records) {
    // 通过 identityId 找到身份，取出 orgId
    const identity = await prisma.userIdentity.findUnique({
      where: { id: record.identityId! },
      select: { orgId: true },
    });
    if (identity?.orgId) {
      await prisma.taskRecord.update({
        where: { id: record.id },
        data: { subjectOrgId: identity.orgId },
      });
    }
  }

  console.log(`已处理 ${records.length} 条记录`);
}
```

> ⚠️ **注意**：此脚本需在**步骤二**上线后、且数据库低峰期运行，建议先在测试环境验证。

---

### 步骤四：调整查询逻辑（后续灰度推进）

完成前三步后，查询逻辑的改造可以逐板块推进。

#### 4.1 管理后台：厅级任务列表/报表

**改动前（按当前主播归属查）：**
```typescript
// 先找主播，再找记录——主播一走，数据就消失
const anchors = await prisma.anchorProfile.findMany({
  where: { hallOrgId: currentHallId }
});
const records = await prisma.taskRecord.findMany({
  where: { subjectUserId: { in: anchors.map(a => a.boundUserId) } }
});
```

**改动后（按记录归属查）：**
```typescript
// 直接根据记录上的归属厅ID查，不管主播现在在哪
const records = await prisma.taskRecord.findMany({
  where: { subjectOrgId: currentHallId }
});
```

#### 4.2 主播端：我的历史任务

**改动前（仅当前身份）：**
```typescript
where: { identityId: currentActiveIdentityId }
```

**改动后（跨身份聚合本人所有记录）：**
```typescript
where: { userId: currentUserId }  // 以人为中心，跨越所有身份
```

---

## 五、审计追踪改造（新增）

### 5.1 数据库模型：新增转岗日志表

在 `schema.prisma` 中新增以下内容：

```prisma
// 1. 在 AnchorStatus 枚举中增加 transferred 终态
enum AnchorStatus {
  unbound
  bound
  inactive
  transferred   // 已转出。专属终态，不可逆转，不可被任何接口重新启用。
}

// 2. 在 AnchorProfile 模型中增加审计关联字段
model AnchorProfile {
  id              String    @id @default(cuid())
  // ... 其他现有字段 ...

  // 审计追踪字段
  isTransferred   Boolean   @default(false)    // 是否是通过转岗生成的身份
  prevProfileId   String?                      // 指向转岗前的旧档案 ID（链式追溯用）
  transferAt      DateTime?                    // 本次入职/转岗的确切时间

  // 关联日志
  transferLogs    AnchorTransferLog[] @relation("ProfileLogs")
}

// 3. 新增专门的转岗历史记录表
model AnchorTransferLog {
  id              String   @id @default(cuid())
  anchorId        String   // 关联的 Anchor ID

  // 变更轨迹
  fromOrgId       String   // 原所属厅 ID
  toOrgId         String   // 新所属厅 ID
  fromProfileId   String   // 原档案 ID（转出方）
  toProfileId     String   // 新档案 ID（转入方）

  // 业务详情
  transferTime    DateTime @default(now())    // 执行时间
  transferReason  String?  @db.Text           // 转岗原因/方案备注
  operatorId      String                      // 执行操作的管理员 ID

  // 关联
  profile         AnchorProfile @relation("ProfileLogs", fields: [toProfileId], references: [id])

  @@index([anchorId])
  @@index([transferTime])
}
```

### 5.2 审计追踪价值说明

| 能力 | 实现方式 |
| :--- | :--- |
| **链式溯源** | 通过 `prevProfileId` 可从当前活跃档案一路追溯到主播最初入职的档案 |
| **责任明确** | `AnchorTransferLog.operatorId` 精确记录谁在何时执行了操作 |
| **方案留痕** | `transferReason` 字段存储转岗协议说明或备注 |
| **时间轴展示** | 基于 `transferTime` 可在后台展示主播的组织归属时间轴 |
| **不影响性能** | 审计数据存于独立表，不会使 `AnchorProfile` 查询变重 |

### 5.3 后续可实现的审查功能

1. **主播轨迹时间轴**：在主播详情页展示「A厅（2026-01~2026-06）→ B厅（2026-06至今）」的可视化轨迹。
2. **转岗报表导出**：导出指定时间段内所有转岗记录，包含原厅、新厅、操作人、转岗原因。
3. **操作人追责**：任何转岗操作均可溯源至具体管理员账号。

---

## 六、bk 标记与账号安全隔离方案（新增）

### 6.1 转岗后的账号状态变更规则

当主播从 A 厅转入 B 厅时，旧账号（留在 A 厅的归档记录）按以下规则处理：

| 字段 | 变更规则 | 示例 |
| :--- | :--- | :--- |
| **昵称** | 追加 `(bk)` 后缀 | `张三` → `张三(bk)` |
| **抖音号** | 追加 `_bk_YYYYMMDD` 后缀 | `DY123` → `DY123_bk_20260617` |
| **抖音 UID** | 追加 `_bk_YYYYMMDD` 后缀（**核心**：释放唯一键占位） | `UID_789` → `UID_789_bk_20260617` |
| **状态** | 改为 `transferred`（专属终态，不可逆） | `active` → `transferred` |

> 日期后缀 `YYYYMMDD` 的作用：防止同一主播多次转岗时，多个 bk 账号之间也产生唯一键冲突。

### 6.2 三层安全保护机制

转岗后的 bk 账号受到三层保护，业务风险被降为零：

| 层级 | 手段 | 效果 |
| :--- | :--- | :--- |
| **第一层：视觉标记** | 昵称加 `(bk)` 后缀，管理列表增加"已转出"标签 | 管理员一眼识别，不会操作错人 |
| **第二层：逻辑锁** | 状态为 `transferred` 时，所有启用/编辑接口报错拒绝 | 接口级别拦截所有重新启用的尝试 |
| **第三层：数据致残** | 抖音 UID 加后缀后，与抖音采集到的实时 UID 永远不匹配 | 即使绕过前两层，该账号也无法采集到任何实时业务数据 |

### 6.3 Service 层逻辑锁实现

**文件：** `backend/src/modules/anchor/service.ts`

在 `toggleProfileStatus`（或类似的启用/停用方法）中增加防御校验：

```typescript
async toggleProfileStatus(profile: any, enable: boolean) {
  // 核心防御：transferred 是不可逆终态，任何接口都不能将其重新激活
  if (profile.status === 'transferred') {
    throw createAnchorBusinessError(
      "CANNOT_REACTIVATE_TRANSFERRED_ANCHOR",
      "该账号已完成转岗归档，属于历史审计数据，禁止重新启用或修改。如有疑问请联系系统管理员。"
    );
  }

  // 原有的启用/停用逻辑...
}
```

同样，在任何**修改 `AnchorProfile` 字段**的接口入口处，均应增加相同的状态前置校验：

```typescript
// 通用前置守卫，建议抽取为独立函数
function assertProfileNotTransferred(profile: AnchorProfile) {
  if (profile.status === 'transferred') {
    throw createAnchorBusinessError(
      "CANNOT_MODIFY_TRANSFERRED_ANCHOR",
      "已转岗归档的账号为只读历史数据，不允许任何字段修改。"
    );
  }
}
```

### 6.4 前端展示策略

1. **主播管理列表**：默认过滤 `status: 'transferred'` 的记录，仅在"历史档案"或"转岗记录"专属模块中展示。
2. **状态标签渲染**：对 `transferred` 状态的行，渲染为灰色"已转出"标签，不渲染任何操作按钮（启用、编辑、删除）。
3. **搜索提示**：当管理员搜索主播昵称时，若同时找到活跃账号和带 `(bk)` 的档案，系统在 bk 账号旁显示"此为已转出的历史档案"提示。

---

## 七、主播日常任务生命周期彻底终结方案（新增）

### 7.1 问题本质分析

主播身份（`UserIdentity` with `roleCode=ANCHOR`）与管理身份（`roleCode=ADMIN`等）是**完全独立**的两条记录，互不关联。因此：

- 废弃旧主播身份**不影响**同一用户的管理身份
- 管理身份的任务权限、操作记录、组织归属完全不受转岗影响

核心问题是：**bk 账号在转岗后必须永久停止产生新的任务数据**，这是它作为历史档案的生命周期终点。

### 7.2 生命周期的三个物理终点

bk 账号的生命周期终结由三个维度共同保证，缺一不可：

| 终点维度 | 操作对象 | 操作内容 | 终结效果 |
| :--- | :--- | :--- | :--- |
| **标识终点** | `AnchorProfile.nickname` | 追加 `(bk)` 后缀 | 视觉上确认账号已归档 |
| **档案终点** | `AnchorProfile.status` | 改为 `transferred`（不可逆终态） | 数据层面彻底锁死，所有修改接口报错拦截 |
| **权限终点** | `UserIdentity.status` | 改为 `disabled` | 从根源切断新任务产生的所有路径 |

**"权限终点"是彻底性的核心**：任务分发引擎 `listAssignmentAudienceMembers` 严格过滤 `UserIdentity.status = "active"`，一旦旧身份被 `disabled`，该主播将从任务受众名单中永久消失，无论是日常任务还是管理式任务，都不会再有新记录产生。

### 7.3 任务分发引擎的深度防御分析

**文件：** `backend/src/modules/task/assignment/daily-assignment.utils.ts`

任务受众解析的核心查询：

```typescript
// 当前代码（已具备基础防御，但存在漏洞）
const identities = await db.userIdentity.findMany({
  where: {
    status: "active",                          // ✅ 已过滤非活跃身份
    roleCode: "ANCHOR",
    scopePath: { startsWith: target.orgPathSnapshot },
    user: { status: "active" },                // ✅ 已过滤非活跃用户
    anchorProfile: { status: { not: "inactive" } },  // ⚠️ 漏洞：仅排除 inactive，未排除 transferred
  },
});
```

**漏洞点**：过滤条件只排除了 `inactive`，如果仅对 `AnchorProfile.status` 标记而未将 `UserIdentity.status` 改为 `disabled`，bk 账号仍会进入任务受众。

**正确的双重防御方案（推荐）**：

方案 A（最简洁，依赖步骤二的 UserIdentity 失效操作）：
```typescript
// 只要步骤二中将 UserIdentity.status 改为 'disabled'，此处无需修改
// UserIdentity.status = "active" 这一过滤条件已足够拦截所有 bk 账号
anchorProfile: { status: { not: "inactive" } }
// → 保持现有逻辑即可，disabled 的身份已被第一个条件过滤
```

方案 B（双重保险，防御纵深更强）：
```typescript
// 在 anchorProfile 过滤中明确排除 transferred 状态
anchorProfile: {
  status: { notIn: ["inactive", "transferred"] }
}
```

> **结论**：步骤二中对 `UserIdentity.status = "disabled"` 的操作是关键。只要这一步到位，分发引擎无需额外改动即可实现完整拦截。方案 B 作为额外防线可选择性添加。

### 7.4 报表统计的"数据幽灵"风险与修复

**文件：** `backend/src/modules/task/report/report.routes.ts`

当前的统计有效性验证函数：

```typescript
// 当前代码（存在漏洞）
function isRecordValid(record: any) {
  if (record.subjectType === "ORG") return true;       // 管理式任务直接通过
  return !record.user || record.user.status === "active"; // 仅验证 User 状态
}
```

**两个风险点：**
1. `User.status` 仍为 `active`（多身份用户的 User 记录本身不会因转岗而改变），旧主播的任务记录会继续计入报表
2. `subjectType === "ORG"` 的管理式任务跳过所有个人状态检查

**修复方案：** 增加对 `UserIdentity.status` 的联动校验：

```typescript
// 修复后的 isRecordValid
function isRecordValid(record: any) {
  if (record.subjectType === "ORG") return true;

  // 校验用户账号状态
  const isUserActive = record.user?.status === "active";
  
  // 关键新增：校验产生记录时的身份是否仍然有效
  // identityId 关联的 UserIdentity.status 为 disabled 时，记录不计入活跃统计
  const isIdentityDisabled = record.identity?.status === "disabled";

  return isUserActive && !isIdentityDisabled;
}
```

> **注意**：此改动需要在报表查询中 `include` 关联的 `identity` 字段。

### 7.5 完整的生命周期事务代码（最终版）

将步骤二的转岗事务代码补充为包含"权限终点"的完整版本：

```typescript
if (nextHallOrgId !== current.hallOrgId) {
  const transferTimestamp = new Date();
  const bkSuffix = `_bk_${transferTimestamp.toISOString().slice(0, 10).replace(/-/g, "")}`;

  // ── 标识终点 ──────────────────────────────────────────────────────
  // 1. 旧档案打 bk 标记，释放唯一键占位，锁定为不可逆终态
  await tx.anchorProfile.update({
    where: { id: current.id },
    data: {
      nickname:   `${current.nickname}(bk)`,
      douyinNo:   current.douyinNo  ? `${current.douyinNo}${bkSuffix}`  : undefined,
      douyinUid:  current.douyinUid ? `${current.douyinUid}${bkSuffix}` : undefined,
      status:     "transferred",  // 档案终点：专属终态，不可逆
    },
  });

  // 2. 同步更新旧 TaskRecord 关联键（历史任务链接不断）
  if (current.douyinUid) {
    await tx.taskRecord.updateMany({
      where: { subjectKey: current.douyinUid },
      data:  { subjectKey: `${current.douyinUid}${bkSuffix}` },
    });
  }

  // ── 权限终点（生命周期彻底终结的核心）───────────────────────────
  // 3. 将旧主播身份物理冻结，从任务分发名单中永久消失
  //    注意：仅操作 roleCode=ANCHOR 且关联旧档案的身份
  //    管理身份（ADMIN等）的 UserIdentity 由于 roleCode 不同，完全不受影响
  await tx.userIdentity.updateMany({
    where: {
      anchorProfileId: current.id,   // 精准定位旧档案关联的身份
      roleCode: "ANCHOR",
      status: "active",
    },
    data: {
      status: "disabled",
      expiredAt: transferTimestamp,  // 记录冻结时间，形成时间切片
    },
  });

  // ── 新身份建立 ────────────────────────────────────────────────────
  // 4. 在新厅为同一用户建立全新的主播身份（不影响旧厅历史数据）
  if (updated.boundUserId) {
    await tx.userIdentity.create({
      data: {
        userId: updated.boundUserId,
        roleCode: "ANCHOR",
        orgId: nextHallOrgId,
        anchorProfileId: updated.id,
        scopePath: nextHallPath,
        status: "active",
      },
    });
  }

  // ── 审计留痕 ──────────────────────────────────────────────────────
  // 5. 写入转岗审计日志
  await tx.anchorTransferLog.create({
    data: {
      anchorId:       anchor.id,
      fromOrgId:      current.hallOrgId,
      toOrgId:        nextHallOrgId,
      fromProfileId:  current.id,
      toProfileId:    updated.id,
      transferTime:   transferTimestamp,
      transferReason: transferReason ?? null,
      operatorId:     currentAdminId,
    },
  });

  // ── 历史数据冻结 ──────────────────────────────────────────────────
  // 6. 不执行 TaskRecord.subjectOrgId 批量更新
  //    历史数据永远归属于原组织，这是正确且不可修改的行为
}
```

### 7.6 生命周期终结后的系统行为验证清单

| 行为 | 预期结果 | 验证方式 |
| :--- | :--- | :--- |
| 日常任务分发 | bk 账号不再出现在任务受众名单中 | 检查 `UserIdentity.status = disabled`，不满足 `active` 过滤 |
| 新任务记录生成 | `ensureDailyRecordForDate` 不会为该身份创建新记录 | 分发名单为空，无触发条件 |
| 管理式任务分发 | 管理员搜索不到 bk 账号 | 管理端搜索接口应过滤 `status: transferred` 档案 |
| 报表统计分母 | 完成率计算的分母不包含 bk 账号 | `isRecordValid` 增加 `identity.status` 校验后生效 |
| 主播端身份列表 | bk 账号不出现在"切换身份"列表 | 身份查询过滤 `status: active` 已天然拦截 |
| 管理员登录 | 同用户的管理身份正常登录，不受任何影响 | 管理身份 `UserIdentity` 记录未被修改 |
| 接口重新启用 | `toggleProfileStatus` 报错拒绝 | `assertProfileNotTransferred` 守卫拦截 |

---

## 八、影响面分析

### 8.1 改动影响矩阵

| 模块 | 影响类型 | 风险等级 | 改动时机 |
| :--- | :--- | :--- | :--- |
| 任务记录生成 | 新增字段写入 | ⬛ 极低 | 立即上线 |
| 主播转岗功能 | 核心逻辑替换 | 🟡 中 | 测试后上线 |
| UserIdentity 冻结 | 新增 disabled 操作 | ⬛ 极低 | 随步骤二上线（关键） |
| 任务分发引擎 | 增加 transferred 过滤（可选） | ⬛ 极低 | 随步骤二上线 |
| 报表统计过滤 | isRecordValid 增加 identity 校验 | 🟢 低 | 随步骤二上线 |
| 厅级任务报表 | 查询条件调整 | 🟡 中 | 灰度切换 |
| 主播端历史任务 | 查询扩大范围 | 🟢 低 | 灰度切换 |
| 管理身份权限 | **不受影响** | ⬛ 零 | 无需改动 |
| 抖音 UID 唯一性 | bk 后缀释放占位 | 🟢 低 | 随步骤二上线 |
| 登录/身份切换 | disabled 身份自动过滤 | ⬛ 极低 | 随步骤二上线（已天然支持） |
| bk 账号防误操作 | 新增逻辑锁 | ⬛ 极低 | 随步骤二上线 |
| 审计日志表 | 新增数据库表 | ⬛ 极低 | 随步骤二上线 |

### 8.2 登录与身份切换的兼容性

改造后小明的账号登录行为：

```
小明登录 App
  ↓
系统查询：SELECT * FROM user_identities WHERE userId=小明ID AND status='active'
  ↓
结果：只返回 B厅的活跃主播身份 + 管理身份（如有）
  ↓
A厅旧主播身份（status=disabled）被自动过滤，用户无感知
  ↓
登录成功，展示 B厅为当前组织；管理功能完全正常
```

> 现有的登录逻辑、身份切换逻辑中一般已有 `status: 'active'` 过滤，无需额外改动。

---

## 九、实施计划

```
Phase 1（立即，0风险）
  └── 修改 record.service.ts：生成任务时写入 subjectOrgId 快照

Phase 2（1-2天，中风险，测试后上线）
  ├── schema.prisma：新增 AnchorStatus.transferred 枚举值
  ├── schema.prisma：新增 AnchorTransferLog 表及 AnchorProfile 审计字段
  ├── 执行 prisma migrate 生成迁移文件
  └── 修改 anchor/service.ts：
        - 转岗改为"bk 标记 + 档案 transferred + 身份 disabled + 新建"完整事务
        - UserIdentity 物理冻结（生命周期权限终点，核心）
        - 写入 AnchorTransferLog 审计记录
        - toggleProfileStatus 增加 transferred 状态防御逻辑

Phase 2.5（随 Phase 2 上线，极低风险）
  ├── 修改 report.routes.ts：isRecordValid 增加 identity.status 校验
  └── 修改 daily-assignment.utils.ts：anchorProfile 过滤增加 transferred 排除（可选双重保险）

Phase 3（1天，后台操作）
  └── 运行存量数据修复脚本，补齐历史 subjectOrgId 空值

Phase 4（逐步，灰度推进）
  ├── 修改厅级任务报表查询逻辑（按 subjectOrgId 查）
  ├── 修改主播端历史任务查询逻辑（按 userId 聚合）
  └── 前端：默认过滤 transferred 状态账号，增加"历史档案"模块
```

---

## 十、不改动的边界说明

以下内容**不在本次改造范围内**，保持原逻辑不变：

1. **厅管任务（HallTaskRecord）**：已有 `hallOrgId` 字段，本身以"厅"为主体，数据归属天然正确。
2. **AnchorProfile.hallOrgId**：档案层面的"当前归属厅"字段仍然更新，用于展示小明当前在哪个厅。区别是：这只是一个"现状"展示字段，不再用于决定历史数据的归属。
3. **管理身份**：`updateMany` 明确限制 `roleCode: "ANCHOR"`，管理身份（ADMIN 等）的 `UserIdentity` 记录完全不会被触碰。

---

## 十一、回滚方案

如果步骤二上线后发现问题，回滚步骤：

1. **代码回滚**：将 `anchor/service.ts`、`report.routes.ts`、`daily-assignment.utils.ts` 回退到改动前版本重新部署。
2. **数据回滚**（如有必要）：
   - 将 Phase 2 期间产生的 `status=transferred` 的档案记录状态恢复，并去除 bk 后缀字段。
   - 将 Phase 2 期间产生的 `status=disabled` 的身份记录重新激活（`status=active`，清空 `expiredAt`）。
   - 删除 Phase 2 期间新建的 B厅身份记录。
   - 删除 Phase 2 期间写入的 `AnchorTransferLog` 记录。
   - 以上操作可通过 `AnchorTransferLog` 表中的记录准确定位需回滚的数据范围。

> 建议在低流量时段（凌晨）上线步骤二，同时备份数据库。

---

*本文档由 AI 辅助生成，最终实施方案以开发团队评审结果为准。*
